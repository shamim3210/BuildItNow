{
  "name": "libraryms-project",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend start",
    "test": "npm --prefix backend test",
    "seed": "npm --prefix backend run seed",
    "add:books": "npm --prefix backend run add:books",
    "bootstrap:librarian": "npm --prefix backend run bootstrap:librarian",
    "seed:rooms": "npm --prefix backend run seed:rooms",
    "reminders": "npm --prefix backend run reminders",
    "digest": "npm --prefix backend run digest"
  },
  "dependencies": {
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0"
  }
}
